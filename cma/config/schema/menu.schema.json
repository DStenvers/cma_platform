{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Menu Configuration",
  "type": "object",
  "required": ["menus"],
  "definitions": {
    "accessLevel": {
      "type": "string",
      "enum": ["user", "admin", "developer"],
      "default": "user",
      "description": "Minimum access level required: user (0), admin (1), developer (2)"
    }
  },
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "description": "Schema version"
    },
    "menus": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "name"],
        "properties": {
          "id": {
            "type": "integer",
            "description": "Unique menu identifier"
          },
          "name": {
            "type": "string",
            "description": "Menu name/title"
          },
          "order": {
            "type": "integer",
            "default": 0,
            "description": "Display order"
          },
          "accessLevel": {
            "$ref": "#/definitions/accessLevel"
          },
          "items": {
            "type": ["array", "null"],
            "description": "Menu items; null on a menu that has none",
            "items": {
              "type": "object",
              "required": ["id", "name"],
              "properties": {
                "id": {
                  "type": "integer",
                  "description": "Unique item identifier"
                },
                "name": {
                  "type": "string",
                  "description": "Menu item display name"
                },
                "form": {
                  "type": ["string", "null"],
                  "description": "Form reference name (legacy key, still read as fallback)"
                },
                "formName": {
                  "type": ["string", "null"],
                  "description": "Form reference name; preferred over form"
                },
                "formId": {
                  "type": ["integer", "string", "null"],
                  "description": "Form reference by numeric id. Holds a name on items converted by the formName migration, and null on an item that opens no form."
                },
                "href": {
                  "type": ["string", "null"],
                  "description": "External URL link"
                },
                "target": {
                  "type": "string",
                  "enum": ["_self", "_blank", "_parent", "_top"],
                  "description": "Link target"
                },
                "order": {
                  "type": "integer",
                  "default": 0,
                  "description": "Display order within menu"
                },
                "visible": {
                  "type": "boolean",
                  "default": true,
                  "description": "Whether item is visible"
                },
                "accessLevel": {
                  "$ref": "#/definitions/accessLevel"
                }
              }
            }
          }
        }
      }
    }
  }
}
